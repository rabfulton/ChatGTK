numpy==2.2.2
openai==1.61.0
requests>=2.0.0
websockets>=12.0
sounddevice==0.4.6
soundfile==0.12.1
PyGObject>=3.42.0; platform_system == "Linux"
beets>=1.6.0

# Optional: Memory feature - required for semantic memory
# qdrant-client>=1.7.0

# Optional: Local embeddings (only needed if not using hosted embeddings)
# sentence-transformers>=2.2.0
